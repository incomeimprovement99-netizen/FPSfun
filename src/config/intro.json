{
  "_note": "The intro (src/ui/intro.ts): the card the page opens on, which is also the loading screen, and the shorter one that plays as you drop into a match. Black screen, green rain falling at a different speed in every column, the name smashed in over it, then a rifle round through the middle: the glass cracks from a bullet hole, a beat of quiet, and then one shotgun blast puts a spread of holes through the whole pane at once before it lets go and falls away into the game. The beats are seconds from the start and each is the moment it begins, so they read in order; `blast` is the shotgun, `out` is when the shards start falling and `end` is when the card is gone. The boot card holds on the rain until the world is in (`waitMost` seconds at the outside), so it covers loading rather than being shown after it; the match one plays over a match that is already starting underneath, so it is short and never in the way. Every beat is skipped by a key or a click, and with `prefers-reduced-motion` the shake, the flash and the falling shards are dropped for a plain fade (`reduced`).",
  "boot": {
    "rain": 0,
    "title": 0.5,
    "shot": 1.75,
    "blast": 2.45,
    "out": 3.1,
    "end": 3.9
  },
  "match": {
    "rain": 0,
    "title": 0.12,
    "shot": 0.6,
    "blast": 0.95,
    "out": 1.35,
    "end": 1.85
  },
  "reduced": {
    "_note": "No shake, no flash, no shards: the card fades in, holds, cracks once and fades out.",
    "end": 1.6
  },
  "text": {
    "_note": "The card's own words. `mark` is the big line and `sub` the one under it, spaced out the way a game's title screen spaces it.",
    "mark": "B00G's FPS",
    "sub": "FULL POWER SURGE"
  },
  "rain": {
    "_note": "The green rainfall. A column is `column` pixels wide, its glyphs `glyph` pixels tall, and it falls at somewhere between `slow` and `fast` glyphs a second, drawn afresh every `swap` seconds so the characters change as they fall. `trail` is how much of the last frame is kept (the tail behind each head), `head` how bright the leading glyph is and `dim` the rest.",
    "column": 16,
    "glyph": 18,
    "slow": 6,
    "fast": 26,
    "swap": 0.07,
    "trail": 0.82,
    "head": "#d9ffe4",
    "dim": "#25c65a"
  },
  "crack": {
    "_note": "The first shot through the screen. `rays` cracks run out from the hole, each wandering by up to `wander` radians; `rings` are the rings of glass between them, the ones that make it read as a pane rather than a star. `hole` is the bullet hole's radius in pixels and `spread` how many seconds the cracks take to run to the edge. `shake` is the screen's kick in pixels at the moment of the shot. `flash` is how white the screen goes at the shot, 0..1: 0.45, half of the 0.9 it was, because the owner found the flashes too bright.",
    "rays": 13,
    "wander": 0.22,
    "branches": 2,
    "rings": 3,
    "hole": 13,
    "spread": 0.35,
    "shake": 26,
    "flash": 0.45
  },
  "wait": {
    "_note": "The boot card holds on the rain and the name until the world is in, so the animation covers loading instead of following it. `waitMost` is the longest it will hold before it fires anyway, because a card that never ends is worse than a card that fires early.",
    "waitMost": 14
  },
  "blast": {
    "_note": "The shotgun blast that finishes the pane: one flash, one kick, and `pellets` holes through the glass in the same instant, thrown out over `spread` of the screen and never into the middle (`keepOut` of the way out, so the pattern is round the name rather than over it, and the rifle round's own hole stays the one in the centre). Each pellet is a smaller hole than the rifle round's with `rays` short cracks at `reach` of its length, so the first shot stays the break that carries the pane. One blast reads as a shotgun; a string of single rounds read as a strobe, which is what this replaced. `flashAlpha` is how white the blast turns the screen, 0..1, and `flash` how long it lasts, s: 0.375, half of the 0.75 it was.",
    "pellets": 18,
    "rays": 4,
    "rings": 1,
    "spread": 0.95,
    "reach": 0.075,
    "hole": 5,
    "kick": 22,
    "flash": 0.14,
    "keepOut": 0.42,
    "flashAlpha": 0.375
  },
  "_modes": "The card that plays as a match starts is the same animation with the mode's own words: `name` is the big line, the mode's name, and `sub` the line under it. The game's own name goes small above them, so every card is still ours. The key is the kind of match (src/game/stats.ts MatchKind; every bot difficulty is `bots`), and a battle royale on Resurgence rules is `resurgence`. A kind not listed plays the boot card's words.",
  "modes": {
    "br": {
      "name": "BATTLE ROYALE",
      "sub": "LAST SQUAD STANDING"
    },
    "resurgence": {
      "name": "RESURGENCE",
      "sub": "FALL AND COME BACK"
    },
    "duel": {
      "name": "1V1",
      "sub": "ONE ROUND AT A TIME"
    },
    "triple": {
      "name": "1V1V1",
      "sub": "THREE WAYS, ONE WINNER"
    },
    "bots": {
      "name": "VS BOTS",
      "sub": "PROVE IT ON THEM FIRST"
    },
    "gunrun": {
      "name": "GUN RUN",
      "sub": "EVERY KILL A NEW GUN"
    },
    "tdm": {
      "name": "TEAM DEATHMATCH",
      "sub": "FIRST TEAM TO THE SCORE"
    },
    "ffa": {
      "name": "FREE FOR ALL",
      "sub": "EVERYONE IS THE ENEMY"
    },
    "crown": {
      "name": "CROWN",
      "sub": "TAKE IT AND HOLD IT"
    },
    "control": {
      "name": "CONTROL",
      "sub": "HOLD THE ZONES"
    },
    "search": {
      "name": "SEARCH",
      "sub": "PLANT IT OR STOP IT"
    }
  },
  "_speedkills": "SpeedKills' own card (src/game/game.ts): its name as the big line, its line under it, and the rain in its two neon colours instead of the green.",
  "speedkills": {
    "mark": "SPEEDKILLS",
    "sub": "MOVE FAST. AIM TRUE.",
    "head": "#e8fbff",
    "dim": "#20e0ff",
    "alt": "#ff2e9a"
  }
}
